__pycache__/
*.pyc
.venv/
node_modules/
.env
.env.*
*.log
.DS_Store
Thumbs.db
teste-*.png
# Repo PUBLICO: nao publicar o dump granular da Rita (gasto por campanha, keywords,
# search terms, perdas do Pipedrive). O pipeline funciona sem commitar: dump e geracao
# rodam no MESMO job da Action (mesmo disco). So o HTML do relatorio (relatorios/) e commitado.
data/rita/
